# Render Node.js test

Run locally with `npm start`, then open http://localhost:3000/ or http://localhost:3000/health.

To deploy, upload these files to a GitHub repository. In Render, create a Web Service connected to that repository. Select Node, use `npm install` as the build command and `npm start` as the start command. Open the assigned onrender.com URL after deployment.
